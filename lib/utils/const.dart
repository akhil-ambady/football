// There are constants that are used in the projects
import 'package:flutter/animation.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

// Palette is the colors that are used
class Palette {
  static const Color logoColor = Color(0xffDBA437);
  static const Color textColor = Color(0xff0C2E8A);
  static const Color red = Color(0xffD62C24);
  static const Color grey = Color(0xff999999);
  static const Color black = Color(0xff000000);
  static const Color white = Color(0xffFFFFFF);
}

// Assets are the images and icons that are needed
class Assets {
  static const String footballLogo = "assets/images/mainLogo.png";
}

// To provide constant properties
class Properties {
  static double commonHorizontalPadding = 15.w;
}
